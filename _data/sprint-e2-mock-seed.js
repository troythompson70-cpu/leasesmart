/**
 * Sprint E2 — legal framework mock config (draft only)
 */
window.SPRINT_E2_MOCK = {
  cookiePolicyVersion: 'e2-draft-2026',
  demoAutoDecline: true,
  gdprRegions: ['EEA', 'UK'],
  ccpaRegions: ['California'],
  consentCheckboxText: 'I confirm this client has consented to having their housing search tracked.',
  attorneyBanner: 'DEMO — Attorney approval required before live use'
};

function e2SeedIfNeeded() {
  /* marker for init — config only */
}
